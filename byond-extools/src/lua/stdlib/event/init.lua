local event = {}



function event.hook(event_name, callback_id, callback)

end

function event.call(event_name, ...)

end

function event.foreach(event_name, ...)

end

function event.call_async(event_name, callback, ...)

end

function event.trigger(event_name, ...)

end

function event.listeners(event_name)

end

return event